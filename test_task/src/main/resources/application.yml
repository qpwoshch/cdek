spring:
  datasource:
    url: jdbc:h2:mem:timetracker;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always

mybatis:
  configuration:
    map-underscore-to-camel-case: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  auth:
    username: student
    password: student123
  jwt:
    secret: 01234567890123456789012345678901
    expiration-minutes: 120
